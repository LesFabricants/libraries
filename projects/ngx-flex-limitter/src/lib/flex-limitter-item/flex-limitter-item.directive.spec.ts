import { FlexLimitterItemDirective } from './flex-limitter-item.directive';

describe('FlexLimitterItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexLimitterItemDirective();
    expect(directive).toBeTruthy();
  });
});
