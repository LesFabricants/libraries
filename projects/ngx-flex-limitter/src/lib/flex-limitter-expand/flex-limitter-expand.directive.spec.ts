import { FlexLimitterExpandDirective } from './flex-limitter-expand.directive';

describe('FlexLimitterExpandDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexLimitterExpandDirective();
    expect(directive).toBeTruthy();
  });
});
