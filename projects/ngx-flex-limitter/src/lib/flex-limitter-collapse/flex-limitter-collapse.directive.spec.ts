import { FlexLimitterCollapseDirective } from './flex-limitter-collapse.directive';

describe('FlexLimitterCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexLimitterCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
